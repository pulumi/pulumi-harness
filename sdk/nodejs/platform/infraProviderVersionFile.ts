// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Resource for uploading files to Terraform/OpenTofu Provider Versions in the IaCM Provider Registry.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as harness from "@pulumi/harness";
 *
 * // First, create a provider
 * const aws = new harness.platform.InfraProvider("aws", {
 *     type: "aws",
 *     description: "AWS provider for IaCM",
 * });
 * // Create a version
 * const awsV5 = new harness.platform.InfraProviderVersion("aws_v5", {
 *     providerId: aws.id,
 *     version: "5.0.0",
 *     gpgKeyId: "your-gpg-key-id",
 *     protocols: [
 *         "5.0",
 *         "6.0",
 *     ],
 * });
 * // Upload files for different platforms
 * const awsLinuxAmd64 = new harness.platform.InfraProviderVersionFile("aws_linux_amd64", {
 *     providerId: aws.id,
 *     version: awsV5.version,
 *     filePath: "./terraform-provider-aws_5.0.0_linux_amd64.zip",
 * });
 * const awsDarwinAmd64 = new harness.platform.InfraProviderVersionFile("aws_darwin_amd64", {
 *     providerId: aws.id,
 *     version: awsV5.version,
 *     filePath: "./terraform-provider-aws_5.0.0_darwin_amd64.zip",
 * });
 * const awsWindowsAmd64 = new harness.platform.InfraProviderVersionFile("aws_windows_amd64", {
 *     providerId: aws.id,
 *     version: awsV5.version,
 *     filePath: "./terraform-provider-aws_5.0.0_windows_amd64.zip",
 * });
 * // Upload SHA256SUMS and signature files
 * const awsShasums = new harness.platform.InfraProviderVersionFile("aws_shasums", {
 *     providerId: aws.id,
 *     version: awsV5.version,
 *     filePath: "./terraform-provider-aws_5.0.0_SHA256SUMS",
 * });
 * const awsShasumsSig = new harness.platform.InfraProviderVersionFile("aws_shasums_sig", {
 *     providerId: aws.id,
 *     version: awsV5.version,
 *     filePath: "./terraform-provider-aws_5.0.0_SHA256SUMS.sig",
 * });
 * // Optional: Override the uploaded filename if different from local filename
 * const customName = new harness.platform.InfraProviderVersionFile("custom_name", {
 *     providerId: aws.id,
 *     version: awsV5.version,
 *     filePath: "./local-file.zip",
 *     filename: "terraform-provider-aws_5.0.0_custom.zip",
 * });
 * ```
 *
 * ## Notes
 *
 * - Files cannot be updated once uploaded. To change a file, you must delete and recreate the resource.
 * - When `filePath` is provided, the resource reads the local file and uploads its content to the provider registry.
 * - The `filename` is automatically derived from `filePath` if not explicitly provided.
 * - Common files to upload include:
 *   - Platform-specific provider binaries (e.g., `terraform-provider-aws_5.0.0_linux_amd64.zip`)
 *   - SHA256SUMS file for verification
 *   - SHA256SUMS.sig signature file for GPG verification
 * - Files are uploaded as raw binary content with `Content-Type: application/octet-stream`.
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import harness:platform/infraProviderVersionFile:InfraProviderVersionFile example <provider_id>/<version>/<filename>
 * ```
 */
export class InfraProviderVersionFile extends pulumi.CustomResource {
    /**
     * Get an existing InfraProviderVersionFile resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InfraProviderVersionFileState, opts?: pulumi.CustomResourceOptions): InfraProviderVersionFile {
        return new InfraProviderVersionFile(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'harness:platform/infraProviderVersionFile:InfraProviderVersionFile';

    /**
     * Returns true if the given object is an instance of InfraProviderVersionFile.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InfraProviderVersionFile {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InfraProviderVersionFile.__pulumiType;
    }

    /**
     * Local path to the file to upload. Required for uploading file content.
     */
    declare public readonly filePath: pulumi.Output<string>;
    /**
     * Name of the file to upload (e.g., terraform-provider-aws*5.0.0*linux*amd64.zip). If not provided, will be derived from file*path. Use this to override the uploaded filename if it should differ from the local filename.
     */
    declare public readonly filename: pulumi.Output<string | undefined>;
    /**
     * The ID of the provider.
     */
    declare public readonly providerId: pulumi.Output<string>;
    /**
     * Indicates if the provider version is synced after file upload.
     */
    declare public /*out*/ readonly synced: pulumi.Output<boolean>;
    /**
     * Provider version number.
     */
    declare public readonly version: pulumi.Output<string>;

    /**
     * Create a InfraProviderVersionFile resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InfraProviderVersionFileArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InfraProviderVersionFileArgs | InfraProviderVersionFileState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InfraProviderVersionFileState | undefined;
            resourceInputs["filePath"] = state?.filePath;
            resourceInputs["filename"] = state?.filename;
            resourceInputs["providerId"] = state?.providerId;
            resourceInputs["synced"] = state?.synced;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as InfraProviderVersionFileArgs | undefined;
            if (args?.filePath === undefined && !opts.urn) {
                throw new Error("Missing required property 'filePath'");
            }
            if (args?.providerId === undefined && !opts.urn) {
                throw new Error("Missing required property 'providerId'");
            }
            if (args?.version === undefined && !opts.urn) {
                throw new Error("Missing required property 'version'");
            }
            resourceInputs["filePath"] = args?.filePath;
            resourceInputs["filename"] = args?.filename;
            resourceInputs["providerId"] = args?.providerId;
            resourceInputs["version"] = args?.version;
            resourceInputs["synced"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InfraProviderVersionFile.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InfraProviderVersionFile resources.
 */
export interface InfraProviderVersionFileState {
    /**
     * Local path to the file to upload. Required for uploading file content.
     */
    filePath?: pulumi.Input<string>;
    /**
     * Name of the file to upload (e.g., terraform-provider-aws*5.0.0*linux*amd64.zip). If not provided, will be derived from file*path. Use this to override the uploaded filename if it should differ from the local filename.
     */
    filename?: pulumi.Input<string>;
    /**
     * The ID of the provider.
     */
    providerId?: pulumi.Input<string>;
    /**
     * Indicates if the provider version is synced after file upload.
     */
    synced?: pulumi.Input<boolean>;
    /**
     * Provider version number.
     */
    version?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a InfraProviderVersionFile resource.
 */
export interface InfraProviderVersionFileArgs {
    /**
     * Local path to the file to upload. Required for uploading file content.
     */
    filePath: pulumi.Input<string>;
    /**
     * Name of the file to upload (e.g., terraform-provider-aws*5.0.0*linux*amd64.zip). If not provided, will be derived from file*path. Use this to override the uploaded filename if it should differ from the local filename.
     */
    filename?: pulumi.Input<string>;
    /**
     * The ID of the provider.
     */
    providerId: pulumi.Input<string>;
    /**
     * Provider version number.
     */
    version: pulumi.Input<string>;
}
