// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Resource for creating and managing Harness AutoStopping alerts. Alerts notify users via email or Slack when events such as warmup failures, cooldown failures, or rule lifecycle changes occur.
 */
export class Alert extends pulumi.CustomResource {
    /**
     * Get an existing Alert resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AlertState, opts?: pulumi.CustomResourceOptions): Alert {
        return new Alert(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'harness:autostopping/alert:Alert';

    /**
     * Returns true if the given object is an instance of Alert.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Alert {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Alert.__pulumiType;
    }

    /**
     * When true, the alert applies to all AutoStopping rules in the account (leave `ruleIdList` empty). Mutually exclusive with `ruleIdList`.
     */
    declare public readonly applicableToAllRules: pulumi.Output<boolean | undefined>;
    /**
     * Whether the alert is enabled.
     */
    declare public readonly enabled: pulumi.Output<boolean | undefined>;
    /**
     * List of event types that trigger the alert (e.g. autostopping*rule*created, autostopping*warmup*failed, autostopping*cooldown*failed).
     */
    declare public readonly events: pulumi.Output<string[]>;
    /**
     * Name of the alert.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Notification recipients. At least one of `email` or `slack` is required (with at least one value).
     */
    declare public readonly recipients: pulumi.Output<outputs.autostopping.AlertRecipients>;
    /**
     * List of AutoStopping rule IDs to apply the alert to. Required when `applicableToAllRules` is false. Mutually exclusive with `applicableToAllRules` = true.
     */
    declare public readonly ruleIdLists: pulumi.Output<number[] | undefined>;

    /**
     * Create a Alert resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AlertArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AlertArgs | AlertState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AlertState | undefined;
            resourceInputs["applicableToAllRules"] = state?.applicableToAllRules;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["events"] = state?.events;
            resourceInputs["name"] = state?.name;
            resourceInputs["recipients"] = state?.recipients;
            resourceInputs["ruleIdLists"] = state?.ruleIdLists;
        } else {
            const args = argsOrState as AlertArgs | undefined;
            if (args?.events === undefined && !opts.urn) {
                throw new Error("Missing required property 'events'");
            }
            if (args?.recipients === undefined && !opts.urn) {
                throw new Error("Missing required property 'recipients'");
            }
            resourceInputs["applicableToAllRules"] = args?.applicableToAllRules;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["events"] = args?.events;
            resourceInputs["name"] = args?.name;
            resourceInputs["recipients"] = args?.recipients;
            resourceInputs["ruleIdLists"] = args?.ruleIdLists;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Alert.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Alert resources.
 */
export interface AlertState {
    /**
     * When true, the alert applies to all AutoStopping rules in the account (leave `ruleIdList` empty). Mutually exclusive with `ruleIdList`.
     */
    applicableToAllRules?: pulumi.Input<boolean>;
    /**
     * Whether the alert is enabled.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * List of event types that trigger the alert (e.g. autostopping*rule*created, autostopping*warmup*failed, autostopping*cooldown*failed).
     */
    events?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Name of the alert.
     */
    name?: pulumi.Input<string>;
    /**
     * Notification recipients. At least one of `email` or `slack` is required (with at least one value).
     */
    recipients?: pulumi.Input<inputs.autostopping.AlertRecipients>;
    /**
     * List of AutoStopping rule IDs to apply the alert to. Required when `applicableToAllRules` is false. Mutually exclusive with `applicableToAllRules` = true.
     */
    ruleIdLists?: pulumi.Input<pulumi.Input<number>[]>;
}

/**
 * The set of arguments for constructing a Alert resource.
 */
export interface AlertArgs {
    /**
     * When true, the alert applies to all AutoStopping rules in the account (leave `ruleIdList` empty). Mutually exclusive with `ruleIdList`.
     */
    applicableToAllRules?: pulumi.Input<boolean>;
    /**
     * Whether the alert is enabled.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * List of event types that trigger the alert (e.g. autostopping*rule*created, autostopping*warmup*failed, autostopping*cooldown*failed).
     */
    events: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Name of the alert.
     */
    name?: pulumi.Input<string>;
    /**
     * Notification recipients. At least one of `email` or `slack` is required (with at least one value).
     */
    recipients: pulumi.Input<inputs.autostopping.AlertRecipients>;
    /**
     * List of AutoStopping rule IDs to apply the alert to. Required when `applicableToAllRules` is false. Mutually exclusive with `applicableToAllRules` = true.
     */
    ruleIdLists?: pulumi.Input<pulumi.Input<number>[]>;
}
