// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.harness;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.harness.TimeSleepArgs;
import com.pulumi.harness.Utilities;
import com.pulumi.harness.inputs.TimeSleepState;
import java.lang.String;
import java.util.Map;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Resource that introduces a time delay during create and/or destroy lifecycle phases.
 * 
 * Useful as a synchronization barrier — resources that `dependsOn` this resource will wait until the sleep completes before proceeding. This avoids race conditions with eventually-consistent resources without resorting to `local-exec` provisioners.
 * 
 * Duration values use Go duration format: `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours). Examples: `&#34;30s&#34;`, `&#34;5m&#34;`, `&#34;1h&#34;`, `&#34;500ms&#34;`.
 * 
 * ## Example Usage
 * 
 * ### Create delay - pause 30s after delegate is created before pipeline runs
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.harness.PlatformDelegateToken;
 * import com.pulumi.harness.PlatformDelegateTokenArgs;
 * import com.pulumi.harness.TimeSleep;
 * import com.pulumi.harness.TimeSleepArgs;
 * import com.pulumi.resources.CustomResourceOptions;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var mydelegate = new PlatformDelegateToken("mydelegate", PlatformDelegateTokenArgs.builder()
 *             .name("my-delegate")
 *             .accountId("YOUR_ACCOUNT_ID")
 *             .build());
 * 
 *         var waitForDelegate = new TimeSleep("waitForDelegate", TimeSleepArgs.builder()
 *             .createDuration("30s")
 *             .build(), CustomResourceOptions.builder()
 *                 .dependsOn(mydelegate)
 *                 .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Wait after destroying a delegate before tearing down dependents
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.harness.PlatformDelegateToken;
 * import com.pulumi.harness.PlatformDelegateTokenArgs;
 * import com.pulumi.harness.TimeSleep;
 * import com.pulumi.harness.TimeSleepArgs;
 * import com.pulumi.resources.CustomResourceOptions;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var mydelegate = new PlatformDelegateToken("mydelegate", PlatformDelegateTokenArgs.builder()
 *             .name("my-delegate")
 *             .accountId("YOUR_ACCOUNT_ID")
 *             .build());
 * 
 *         var drainDelegate = new TimeSleep("drainDelegate", TimeSleepArgs.builder()
 *             .destroyDuration("10s")
 *             .build(), CustomResourceOptions.builder()
 *                 .dependsOn(mydelegate)
 *                 .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Both create and destroy delays
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.harness.TimeSleep;
 * import com.pulumi.harness.TimeSleepArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var wait = new TimeSleep("wait", TimeSleepArgs.builder()
 *             .createDuration("30s")
 *             .destroyDuration("10s")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 */
@ResourceType(type="harness:index/timeSleep:TimeSleep")
public class TimeSleep extends com.pulumi.resources.CustomResource {
    /**
     * Duration to sleep when the resource is created. Accepts Go duration strings: `ms`, `s`, `m`, `h`. Example: `&#34;30s&#34;`, `&#34;5m&#34;`. At least one of `createDuration` or `destroyDuration` must be set.
     * 
     */
    @Export(name="createDuration", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> createDuration;

    /**
     * @return Duration to sleep when the resource is created. Accepts Go duration strings: `ms`, `s`, `m`, `h`. Example: `&#34;30s&#34;`, `&#34;5m&#34;`. At least one of `createDuration` or `destroyDuration` must be set.
     * 
     */
    public Output<Optional<String>> createDuration() {
        return Codegen.optional(this.createDuration);
    }
    /**
     * Duration to sleep when the resource is destroyed. Accepts Go duration strings: `ms`, `s`, `m`, `h`. Example: `&#34;30s&#34;`, `&#34;5m&#34;`. At least one of `createDuration` or `destroyDuration` must be set.
     * 
     */
    @Export(name="destroyDuration", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> destroyDuration;

    /**
     * @return Duration to sleep when the resource is destroyed. Accepts Go duration strings: `ms`, `s`, `m`, `h`. Example: `&#34;30s&#34;`, `&#34;5m&#34;`. At least one of `createDuration` or `destroyDuration` must be set.
     * 
     */
    public Output<Optional<String>> destroyDuration() {
        return Codegen.optional(this.destroyDuration);
    }
    /**
     * Arbitrary map of string values. Any change to this map forces replacement of the resource, re-triggering the create and destroy delays.
     * 
     */
    @Export(name="triggers", refs={Map.class,String.class}, tree="[0,1,1]")
    private Output</* @Nullable */ Map<String,String>> triggers;

    /**
     * @return Arbitrary map of string values. Any change to this map forces replacement of the resource, re-triggering the create and destroy delays.
     * 
     */
    public Output<Optional<Map<String,String>>> triggers() {
        return Codegen.optional(this.triggers);
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public TimeSleep(java.lang.String name) {
        this(name, TimeSleepArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public TimeSleep(java.lang.String name, @Nullable TimeSleepArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public TimeSleep(java.lang.String name, @Nullable TimeSleepArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("harness:index/timeSleep:TimeSleep", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private TimeSleep(java.lang.String name, Output<java.lang.String> id, @Nullable TimeSleepState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("harness:index/timeSleep:TimeSleep", name, state, makeResourceOptions(options, id), false);
    }

    private static TimeSleepArgs makeArgs(@Nullable TimeSleepArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? TimeSleepArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .pluginDownloadURL("github://api.github.com/pulumi")
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static TimeSleep get(java.lang.String name, Output<java.lang.String> id, @Nullable TimeSleepState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new TimeSleep(name, id, state, options);
    }
}
