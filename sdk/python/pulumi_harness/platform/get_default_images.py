# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = [
    'GetDefaultImagesResult',
    'AwaitableGetDefaultImagesResult',
    'get_default_images',
    'get_default_images_output',
]

@pulumi.output_type
class GetDefaultImagesResult:
    """
    A collection of values returned by getDefaultImages.
    """
    def __init__(__self__, id=None, images=None, infra_type=None, kind=None, type=None):
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if images and not isinstance(images, dict):
            raise TypeError("Expected argument 'images' to be a dict")
        pulumi.set(__self__, "images", images)
        if infra_type and not isinstance(infra_type, str):
            raise TypeError("Expected argument 'infra_type' to be a str")
        pulumi.set(__self__, "infra_type", infra_type)
        if kind and not isinstance(kind, str):
            raise TypeError("Expected argument 'kind' to be a str")
        pulumi.set(__self__, "kind", kind)
        if type and not isinstance(type, str):
            raise TypeError("Expected argument 'type' to be a str")
        pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def images(self) -> Mapping[str, _builtins.str]:
        """
        Map of image field names to image tag values.
        """
        return pulumi.get(self, "images")

    @_builtins.property
    @pulumi.getter(name="infraType")
    def infra_type(self) -> Optional[_builtins.str]:
        """
        The infrastructure type passed to the execution config API (e.g. `K8`, `VM`). Defaults to `K8`.
        """
        return pulumi.get(self, "infra_type")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> _builtins.str:
        """
        The service kind. Supported values: `ci`, `iacm`, `idp`.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        The configuration type to retrieve. `default` returns Harness-managed default image tags; `customer` returns only the image fields that have been explicitly overridden at the account level. Defaults to `default`.
        """
        return pulumi.get(self, "type")


class AwaitableGetDefaultImagesResult(GetDefaultImagesResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetDefaultImagesResult(
            id=self.id,
            images=self.images,
            infra_type=self.infra_type,
            kind=self.kind,
            type=self.type)


def get_default_images(infra_type: Optional[_builtins.str] = None,
                       kind: Optional[_builtins.str] = None,
                       type: Optional[_builtins.str] = None,
                       opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetDefaultImagesResult:
    """
    Data source for retrieving Harness default execution images for CI, IACM, or IDP.

    ## The `type` Field

    The `type` field controls which set of images is returned:

    - **`default`** (default) — returns the Harness-managed default image tags for the account. These are the images Harness ships and updates over time.
    - **`customer`** — returns only the image fields that have been explicitly overridden at the account level using the `platform.DefaultImages` resource. Fields that have not been overridden are omitted from the `images` map.

    Use `type = "customer"` after applying overrides to verify that the correct custom image tags are in effect.

    ## Example Usage

    ### CI — read Harness default images

    ```python
    import pulumi
    import pulumi_harness as harness

    ci_defaults = harness.platform.get_default_images(kind="ci")
    pulumi.export("ciDefaultImages", ci_defaults.images)
    ```

    ### CI — read customer-configured overrides

    ```python
    import pulumi
    import pulumi_harness as harness

    ci_customer = harness.platform.get_default_images(kind="ci",
        type="customer")
    pulumi.export("ciLiteEngineOverride", ci_customer.images["liteEngineTag"])
    ```

    ### IACM — read Harness default images

    ```python
    import pulumi
    import pulumi_harness as harness

    iacm_defaults = harness.platform.get_default_images(kind="iacm")
    pulumi.export("iacmDefaultImages", iacm_defaults.images)
    ```

    ### IACM — read customer-configured overrides

    ```python
    import pulumi
    import pulumi_harness as harness

    iacm_customer = harness.platform.get_default_images(kind="iacm",
        type="customer")
    pulumi.export("iacmAwsCdkOverride", iacm_customer.images["iacmAwsCdk"])
    ```

    ### IDP — read Harness default images

    ```python
    import pulumi
    import pulumi_harness as harness

    idp_defaults = harness.platform.get_default_images(kind="idp")
    pulumi.export("idpDefaultImages", idp_defaults.images)
    ```

    ### IDP — read customer-configured overrides

    ```python
    import pulumi
    import pulumi_harness as harness

    idp_customer = harness.platform.get_default_images(kind="idp",
        type="customer")
    pulumi.export("idpRegisterCatalogOverride", idp_customer.images["registerCatalog"])
    ```


    :param _builtins.str infra_type: The infrastructure type passed to the execution config API (e.g. `K8`, `VM`). Defaults to `K8`.
    :param _builtins.str kind: The service kind. Supported values: `ci`, `iacm`, `idp`.
    :param _builtins.str type: The configuration type to retrieve. `default` returns Harness-managed default image tags; `customer` returns only the image fields that have been explicitly overridden at the account level. Defaults to `default`.
    """
    __args__ = dict()
    __args__['infraType'] = infra_type
    __args__['kind'] = kind
    __args__['type'] = type
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('harness:platform/getDefaultImages:getDefaultImages', __args__, opts=opts, typ=GetDefaultImagesResult).value

    return AwaitableGetDefaultImagesResult(
        id=pulumi.get(__ret__, 'id'),
        images=pulumi.get(__ret__, 'images'),
        infra_type=pulumi.get(__ret__, 'infra_type'),
        kind=pulumi.get(__ret__, 'kind'),
        type=pulumi.get(__ret__, 'type'))
def get_default_images_output(infra_type: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                              kind: Optional[pulumi.Input[_builtins.str]] = None,
                              type: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                              opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetDefaultImagesResult]:
    """
    Data source for retrieving Harness default execution images for CI, IACM, or IDP.

    ## The `type` Field

    The `type` field controls which set of images is returned:

    - **`default`** (default) — returns the Harness-managed default image tags for the account. These are the images Harness ships and updates over time.
    - **`customer`** — returns only the image fields that have been explicitly overridden at the account level using the `platform.DefaultImages` resource. Fields that have not been overridden are omitted from the `images` map.

    Use `type = "customer"` after applying overrides to verify that the correct custom image tags are in effect.

    ## Example Usage

    ### CI — read Harness default images

    ```python
    import pulumi
    import pulumi_harness as harness

    ci_defaults = harness.platform.get_default_images(kind="ci")
    pulumi.export("ciDefaultImages", ci_defaults.images)
    ```

    ### CI — read customer-configured overrides

    ```python
    import pulumi
    import pulumi_harness as harness

    ci_customer = harness.platform.get_default_images(kind="ci",
        type="customer")
    pulumi.export("ciLiteEngineOverride", ci_customer.images["liteEngineTag"])
    ```

    ### IACM — read Harness default images

    ```python
    import pulumi
    import pulumi_harness as harness

    iacm_defaults = harness.platform.get_default_images(kind="iacm")
    pulumi.export("iacmDefaultImages", iacm_defaults.images)
    ```

    ### IACM — read customer-configured overrides

    ```python
    import pulumi
    import pulumi_harness as harness

    iacm_customer = harness.platform.get_default_images(kind="iacm",
        type="customer")
    pulumi.export("iacmAwsCdkOverride", iacm_customer.images["iacmAwsCdk"])
    ```

    ### IDP — read Harness default images

    ```python
    import pulumi
    import pulumi_harness as harness

    idp_defaults = harness.platform.get_default_images(kind="idp")
    pulumi.export("idpDefaultImages", idp_defaults.images)
    ```

    ### IDP — read customer-configured overrides

    ```python
    import pulumi
    import pulumi_harness as harness

    idp_customer = harness.platform.get_default_images(kind="idp",
        type="customer")
    pulumi.export("idpRegisterCatalogOverride", idp_customer.images["registerCatalog"])
    ```


    :param _builtins.str infra_type: The infrastructure type passed to the execution config API (e.g. `K8`, `VM`). Defaults to `K8`.
    :param _builtins.str kind: The service kind. Supported values: `ci`, `iacm`, `idp`.
    :param _builtins.str type: The configuration type to retrieve. `default` returns Harness-managed default image tags; `customer` returns only the image fields that have been explicitly overridden at the account level. Defaults to `default`.
    """
    __args__ = dict()
    __args__['infraType'] = infra_type
    __args__['kind'] = kind
    __args__['type'] = type
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('harness:platform/getDefaultImages:getDefaultImages', __args__, opts=opts, typ=GetDefaultImagesResult)
    return __ret__.apply(lambda __response__: GetDefaultImagesResult(
        id=pulumi.get(__response__, 'id'),
        images=pulumi.get(__response__, 'images'),
        infra_type=pulumi.get(__response__, 'infra_type'),
        kind=pulumi.get(__response__, 'kind'),
        type=pulumi.get(__response__, 'type')))
